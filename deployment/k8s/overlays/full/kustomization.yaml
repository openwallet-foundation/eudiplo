apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Full Overlay
# Use case: Enterprise production deployments
# Components: EUDIPLO + PostgreSQL + RustFS + HashiCorp Vault

resources:
  - ../../base

namespace: eudiplo

components:
  - ../../components/postgres
  - ../../components/rustfs
  - ../../components/vault
