apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Standard Overlay
# Use case: Staging environments, small production deployments
# Components: EUDIPLO + PostgreSQL + RustFS

resources:
  - ../../base

namespace: eudiplo

components:
  - ../../components/postgres
  - ../../components/rustfs
