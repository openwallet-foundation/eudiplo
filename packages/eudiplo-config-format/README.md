# @eudiplo/config-format

Canonical portable-config identity, validation, migration, atomic file I/O, and
config-bundle (zip) logic shared by `@eudiplo/backend` and `@eudiplo/cli`.

This package is generated in part from `schemas/v*/` — run `pnpm schemas:sync`
from the repository root after changing versioned config schema snapshots.
