{
    "id": "claims-provider",
    "name": "Claims Provider",
    "description": "Provides dynamic claim values during credential issuance",
    "url": "${ATTRIBUTE_PROVIDER_URL:http://localhost:8787}",
    "auth": {
        "type": "none"
    }
}